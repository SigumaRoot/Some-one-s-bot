import Locator from "../classes/Locator";
import Validator from "../classes/Validator";
import { iValidationResult } from "..";
export default class ClassValidator<T extends new () => any> extends Validator<T> {
    static readonly NOT_INSTANCE = "Value isn't an instance of the defined class";
    private readonly clazz;
    constructor(clazz: T);
    validate(data: any, locator: Locator): iValidationResult<T>;
}
