"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Validator_1 = __importDefault(require("../classes/Validator"));
class NumberValidator extends Validator_1.default {
    static NOT_AMONG_NUMBERS = `Value doesn't match anything in the defined set of numbers`;
    numbers;
    constructor(numbers) {
        super();
        this.numbers = numbers;
        if (numbers.length === 0) {
            this.schema = `{"$type":"number"}`;
        }
        else {
            this.schema = `{"$type":"number","$values":[${numbers.join(",")}]}`;
        }
    }
    validate(data, locator) {
        if (typeof data !== "number") {
            return this.failure(locator, Validator_1.default.WRONG_TYPE, this, data);
        }
        if (this.numbers.length > 0) {
            if (!this.numbers.includes(data)) {
                return this.failure(locator, NumberValidator.NOT_AMONG_NUMBERS, this, data);
            }
        }
        return this.success(data);
    }
}
exports.default = NumberValidator;
