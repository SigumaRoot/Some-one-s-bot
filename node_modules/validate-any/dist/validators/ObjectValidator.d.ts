import Locator from "../classes/Locator";
import Validator from "../classes/Validator";
import { iValidationResult } from "..";
export default class ObjectValidator<T> extends Validator<T> {
    static readonly MISSING_PROPERTY = "Object requires this property but is missing";
    static readonly UNKNOWN_PROPERTY = "Object has unknown property which is defined";
    private readonly rule_object?;
    constructor(rule_object?: {
        [property: string]: Validator<T>;
    });
    validate(data: any, locator: Locator): iValidationResult<T>;
}
