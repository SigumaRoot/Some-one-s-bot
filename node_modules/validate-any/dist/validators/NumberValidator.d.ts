import Locator from "../classes/Locator";
import Validator from "../classes/Validator";
import { iValidationResult } from "..";
export default class NumberValidator<T extends number> extends Validator<T> {
    static readonly NOT_AMONG_NUMBERS = "Value doesn't match anything in the defined set of numbers";
    private readonly numbers;
    constructor(numbers: T[]);
    validate(data: any, locator: Locator): iValidationResult<T>;
}
