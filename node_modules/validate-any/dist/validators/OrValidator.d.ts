import Locator from "../classes/Locator";
import Validator from "../classes/Validator";
import { iValidationResult } from "..";
export default class OrValidator<T> extends Validator<T> {
    static readonly NOT_AMONG_RULES = "Value does not match any of the validators defined";
    private readonly validators;
    constructor(validators: Validator<T>[]);
    validate(data: any, locator: Locator): iValidationResult<T>;
}
