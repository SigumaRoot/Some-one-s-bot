import Locator from "../classes/Locator";
import Validator from "../classes/Validator";
import { iValidationResult } from "..";
export default class StringValidator<T extends string | RegExp> extends Validator<T> {
    static readonly NO_REGEX_MATCH = "Value does not match the defined RegExp pattern";
    static readonly NOT_AMONG_STRINGS = "Value doesn't match anything in the defined set of strings";
    private readonly rules;
    constructor(rules: T[]);
    validate(data: any, locator: Locator): iValidationResult<T>;
}
